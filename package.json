{
  "name": "telebot",
  "version": "0.0.1",
  "description": "chatgpt_telegram_bot",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "docker": "docker-compose up -d",
    "setup": "python3 -m venv .venv && source .venv/bin/activate && python3 -m pip install -r requirements.txt",
    "dev": "npx nodemon --exec python3 src/main.py"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "nodemon": "^3.0.2"
  }
}
